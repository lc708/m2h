# pnpm 配置文件
# 启用严格的对等依赖检查
strict-peer-dependencies=false

# 使用符号链接节省磁盘空间
symlink=true

# 启用 shamefully-hoist 来解决一些包的兼容性问题
shamefully-hoist=true

# 自动安装对等依赖
auto-install-peers=true

# 优先使用工作区内的包
prefer-workspace-packages=true

# 设置注册表（可选，默认是 npm）
# registry=https://registry.npmmirror.com/ 